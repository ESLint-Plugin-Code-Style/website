export enum BadgeVariantEnum {
    DEFAULT = "default",
    INFO = "info",
    LINT_ERROR = "lintError",
    LINT_INFO = "lintInfo",
    LINT_PASS = "lintPass",
    LINT_WARN = "lintWarn",
    PURPLE = "purple",
    SUCCESS = "success",
    WARNING = "warning",
}

export enum ButtonTypeEnum {
    BUTTON = "button",
    RESET = "reset",
    SUBMIT = "submit",
}

export enum CardAsEnum {
    ARTICLE = "article",
    DIV = "div",
    SECTION = "section",
}

export enum CardVariantEnum {
    NOTCHED = "notched",
    NOTE = "note",
    TAB = "tab",
}

export enum CodeLanguageEnum {
    BASH = "bash",
    JAVASCRIPT = "javascript",
    JS = "js",
    TEXT = "text",
}

export enum EventNameEnum {
    CHANGE = "change",
    KEYDOWN = "keydown",
    MOUSEDOWN = "mousedown",
    OFFLINE = "offline",
    ONLINE = "online",
    SCROLL = "scroll",
}

export enum InputTypeEnum {
    BUTTON = "button",
    TEXT = "text",
}

export enum KeyboardKeyEnum {
    ARROW_DOWN = "ArrowDown",
    ARROW_UP = "ArrowUp",
    ENTER = "Enter",
    ESCAPE = "Escape",
    KEY_K = "k",
}

export enum LintAccentNameEnum {
    ERROR = "error",
    INFO = "info",
    PASS = "pass",
    VIOLET = "violet",
    WARN = "warn",
}

export enum LintButtonSizeEnum {
    LG = "lg",
    MD = "md",
    SM = "sm",
}

export enum LintButtonToneEnum {
    GHOST = "ghost",
    PRIMARY = "primary",
    SECONDARY = "secondary",
}

export enum LintStatusEnum {
    ERROR = "error",
    INFO = "info",
    PASS = "pass",
    WARN = "warn",
}

export enum MotionRepeatTypeEnum {
    LOOP = "loop",
    MIRROR = "mirror",
    REVERSE = "reverse",
}

export enum SitemapChangeFrequencyEnum {
    ALWAYS = "always",
    DAILY = "daily",
    HOURLY = "hourly",
    MONTHLY = "monthly",
    NEVER = "never",
    WEEKLY = "weekly",
    YEARLY = "yearly",
}

export enum SquiggleVariantEnum {
    ERROR = "error",
    FIX = "fix",
}

export enum ThemeEnum {
    DARK = "dark",
    LIGHT = "light",
    SYSTEM = "system",
}
